Internal Project Management System – (Real-Time Collaboration) You are building an internal project management tool for a growing company. Key Challenge The system must support real-time task updates across multiple users working on the same project. Example: ● User A moves a task from Todo → In Progress ● User B (already logged in) should see the update instantly ● User C opens the project later → should see the latest state Mandatory Tech Stack Backend ● Node.js + Express ● MongoDB ● JWT Authentication ● WebSockets (Socket.IO or equivalent) ● Redis Frontend ● React or Next.js ● State management (Context / Redux / Zustand – choice must be justified) DevOps & Infra  If you have a VM then deploy this way otherwise you can use free services like netlify , render etc ● Backend deployed on non-AWS/GCP VM (e.g. DigitalOcean / Hetzner / Vultr) ● Nginx as reverse proxy ● Domain configuration (subdomain is fine) ● SSL setup (Let’s Encrypt) ● GitHub Actions CI pipeline Phase 1: Planning (MANDATORY – No Coding) A. Functional Requirement Document (FRD) 1–2 pages max Must include: ● Core features ● User roles & permissions ● Assumptions ● Out-of-scope items B. System Design (VERY IMPORTANT) Must include: ● High-level architecture diagram ● API list (endpoint + purpose) ● Database schema (ER or collection design) ● Real-time communication strategy ● Why this approach was chosen ● Scalability considerations Skipping design = rejection Phase 2: Backend Implementation Requirements ● Auth (JWT) ● CRUD: Projects & Tasks ● Task status updates (real-time) ● Socket authentication ● Centralized error handling ● Input validation ● Clean service-layer architecture Mandatory Folder Structure src/ ├─ controllers/ ├─ services/ ├─ routes/ ├─ models/ ├─ sockets/ ├─ middleware/ ├─ config/ └─ app.js Phase 3: Frontend Implementation Screens ● Login ● Project list ● Task board (with real-time updates) Must Demonstrate ● Reusable components ● Controlled forms ● Real-time UI updates ● Clean API + socket abstraction layer ● Error & loading handling Phase 4: DevOps & Deployment If you have a VM then deploy this way otherwise you can use free services like netlify , render etc Backend ● Deployed on VM ● Nginx reverse proxy ● SSL enabled ● Environment variables managed properly ● MongoDB connection secured CI/CD ● GitHub Actions pipeline ● Lint + build + deploy steps ● Proper branching strategy explained Code Quality Expectations ● Clean commits ● No hardcoded values ● Proper folder structure ● Readable, formatted code Documentation (MANDATORY) README must include: ● Architecture overview ● Design decisions & trade-offs ● API list ● Socket events list ● Local setup ● Deployment steps ● URLs AI Usage Declaration (Mandatory) Use AI where ever you want to for anything  however  If you can’t explain AI-generated code → rejection Loom Video (8–10 Minutes) Must explain: 1. System architecture & flow 2. Real-time communication design 3. One backend API (line-by-line) 4. One socket flow (event lifecycle) 5. Deployment & infra setup 6. One technical mistake & fix Deliverables ● GitHub repository link ● Deployed frontend URL ● Deployed backend URL ● Planning & design documents (links or files) ● Loom video link
